package ir.greendex.mafia.game.nato.listeners

interface NatoSocketListener {
    fun onRoomId(id:String)
    fun onMafiaSpeech(it:String)
    fun onMafiaSpeechEnd()
    fun onUsersData(it:String)
    fun onGameEvent(event:String)
    fun onUserAction(it:String)
    fun onSpeechTurn(it:String)
    fun onStartSpeech()
    fun onSpeechTimeUp(it:String)
    fun onVoteToPlayer(it:String)
    fun onReport(it:String)
    fun onAcceptChallenge()
    fun onMafiaVisitation(it:String)
    fun onUseAbility(it:String)
    fun onMafiaShot(it:String)
    fun onCurrentSpeech(it:String)
    fun onCurrentSpeechEnd(it:String)
    fun onMafiaDecision(it:String)
    fun onDetectiveInquiry(it:String)
    fun onUsersChallengeStatus(it:String)
    fun onUsingSpeechOptions(it:String)
    fun onWhichUserRequestSpeechOptions(it:String)
    fun onSpeechOptionMsg(it:String)
    fun onRequestSpeechOptions(it:String)
    fun onReportGun()
    fun onGunStatus(it:String)
    fun onDayUsingGun(it:String)
    fun onDayUsedGun(it:String)
    fun onDayInquiry(it:String)
    fun onDayInquiryResult(it:String)
    fun onChaosVoteTimeNotification()
    fun onChaosVote(it:String)
    fun onChaosVoteResult(it:String)
    fun onChaosUserSpeech(it:String)
    fun onChaosAllSpeech()
    fun onChaosAllSpeechEnd()
    fun onChaosTurnToShake(it:String)
    fun onUsersCharacters(it:String)
    fun onModeratorData(it:String)
    fun onModeratorStatus(it:String)
    fun onModeratorPanelEvent(it:String)
    fun onEndGameResult(it:String)
    fun onLastDecision(it:String)
    fun onPlayerShowCharacter()
    fun onAbandon()
    fun onActionEnd()
    fun onPlayVoice(it:String)
}